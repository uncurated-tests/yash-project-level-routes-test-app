/** @type {import('next').NextConfig} */
const nextConfig = {
  // We leave routing config empty here since we're testing project-level routes
  // which override/augment these via the API
};

module.exports = nextConfig;
